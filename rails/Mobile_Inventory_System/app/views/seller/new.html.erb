<%= form_for @seller, url: {action: "create"} do |f| %>
  <label>SELLER-NAME:			</label><%= f.text_field :name %><br />
  <%#= f.collection_select :brand_id, @brands, :id, :name %>
  <%#= f.text_area :body, size: "60x12" %>
  <%= f.submit "Create" %>
<% end %>